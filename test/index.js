import assert from 'assert';
import webpDetector from '../src/';

describe('webpDetector', () => {
  it('it should get webp img src', done => {
    // webpDetector.isSupportWebp();

    // const imgSrc = webpDetector.getWebpSrc('demo.jpg');
    // const imgSrcTwo = webpDetector.getWebpSrc('demo.jpg', '.webp');

    // assert(window.localStorage && typeof localStorage === 'object' === true, 'support Webp');
    // assert(imgSrc === 'demo.jpg_.webp', 'get');
    // assert(imgSrcTwo === 'demo.jpg.webp', 'get');
    done();
  });
});
